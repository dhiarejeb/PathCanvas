apiVersion: v1
kind: Service
metadata:
  name: pathcanvas-backend-service
spec:
  selector:
    app: pathcanvas-backend
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP   # only accessible inside cluster
